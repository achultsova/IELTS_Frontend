import React from 'react'
import { Box } from '@mui/material'

const ChangeInfo = () => {
    return (
        <Box sx={{
            minHeight: '100vh',
            display: 'flex',
            alignItems: 'center',
            justifyContent: 'center'
        }}>
            Change user info
        </Box>
    )
}

export default ChangeInfo